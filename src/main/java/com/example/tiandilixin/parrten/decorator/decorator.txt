装饰器模式：
装饰器模式的核心就是在不更改原有类基础的情况下给类新增功能。虽然继承也能实现同样的功能，但是如果一直使用继承随着业务的迭代，会导致实现类不断变多，代码量不断膨胀，不易于扩展。
比我我们的SSO拦截器，一开始可能就很简单的一个校验，后来随着功能的迭代，这个拦截器要做的事情越来越多，首先要保证不能影响以前的代码，就可以通过装饰器模式进行扩展，
核心处理部分是：通过一个抽象类继承处理接口，提供一个构造函数，覆盖原来的方法。
装饰类和被装饰类可以独立发展，不会相互耦合。
